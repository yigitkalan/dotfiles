# i3 config file (v4)
# Auto-generated by i3-config-wizard(1). Edited manually.
# See https://i3wm.org/docs/userguide.html for reference.
# Last updated: August 05, 2025

###############################################################################
# General Settings
###############################################################################
set $mod Mod4

# Font for window titles and bar (unless overridden in bar block)
font pango:FiraCode Nerd Font Medium 10

# Hide edge borders and set gaps
# hide_edge_borders both
gaps inner 8
gaps outer 0

default_border pixel 2
default_floating_border pixel 2

# Modifier for dragging floating windows
floating_modifier $mod

# Enable drag-to-move for tiling windows
tiling_drag modifier titlebar

###############################################################################
# Startup Applications
###############################################################################
# Start XDG autostart .desktop files
exec --no-startup-id dex --autostart --environment i3

exec --no-startup-id nitrogen --restore

exec_always --no-startup-id setxkbmap -layout us,tr

#Kde connect
exec --no-startup-id killall -q kdeconnectd; kdeconnectd
exec --no-startup-id killall -q kdeconnect-indicator; sleep 2 && kdeconnect-indicator

# Clipboard manager
exec --no-startup-id greenclip daemon >/dev/null

# Screen lock and suspend
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork --color=#111111

# Polkit agent for GUI password popups
exec_always --no-startup-id /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1

# NetworkManager applet
exec --no-startup-id nm-applet

#picom
# exec_always --no-startup-id picom -b

###############################################################################
# Keybindings
###############################################################################
# Terminal
bindsym $mod+Return exec ghostty

# Window Management
bindsym $mod+q kill
bindsym $mod+f fullscreen toggle
bindsym $mod+Shift+f floating toggle
bindsym $mod+a focus parent
bindsym $mod+o split h
bindsym $mod+v split v
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split
bindsym $mod+r mode "resize"

# Focus Navigation
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# Move Focused Window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# Workspaces
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# Move workspaces between outputs
bindsym $mod+m move workspace to output right
bindsym $mod+n move workspace to output left

# Rofi Applications
bindsym $mod+d exec --no-startup-id rofi -show drun
bindsym $mod+shift+d exec --no-startup-id rofi -show emoji
bindsym $mod+control+d exec --no-startup-id rofi -modi "clipboard:greenclip print" -show clipboard
bindsym $mod+Mod1+d exec --no-startup-id rofi -show calc -modi calc -no-show-match -no-sort

# Audio Controls
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && pkill -RTMIN+10 i3blocks
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && pkill -RTMIN+10 i3blocks
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && pkill -RTMIN+10 i3blocks
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Keyboard Brightness
bindsym XF86KbdLightOnOff exec --no-startup-id brightnessctl --device='rgb:kbd_backlight' set 0
bindsym XF86KbdBrightnessDown exec --no-startup-id brightnessctl --device='rgb:kbd_backlight' set 10%-
bindsym XF86KbdBrightnessUp exec --no-startup-id brightnessctl --device='rgb:kbd_backlight' set 10%+

# Display Brightness
bindsym XF86MonBrightnessDown exec --no-startup-id brightnessctl --min-val=2 -q set 5%-
bindsym XF86MonBrightnessUp exec --no-startup-id brightnessctl -q set 5%+

# Touchpad Toggle
bindsym XF86TouchpadToggle exec --no-startup-id /home/sy/.utils/toggle_touchpad.sh

# Screenshots
bindsym $mod+p exec --no-startup-id maim -o --select "$HOME/Pictures/screenshots/$(date).png"
bindsym Shift+$mod+p exec --no-startup-id maim -o --select | xclip -selection clipboard -t image/png

# Keyboard Layout Switch
bindsym $mod+space exec --no-startup-id xkb-switch -n && pkill -RTMIN+12 i3blocks

# Resize Mode
mode "resize" {
    bindsym h resize shrink width 10 px or 10 ppt
    bindsym j resize grow height 10 px or 10 ppt
    bindsym k resize shrink height 10 px or 10 ppt
    bindsym l resize grow width 10 px or 10 ppt
    bindsym Left resize shrink width 10 px or 10 ppt
    bindsym Down resize grow height 10 px or 10 ppt
    bindsym Up resize shrink height 10 px or 10 ppt
    bindsym Right resize grow width 10 px or 10 ppt
    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+r mode "default"
}

# Configuration Management
bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart

# Exit i3 with Confirmation
bindsym $mod+Shift+q exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

#Locking
bindsym $mod+Mod1+l exec i3lock --nofork --color=#111111

###############################################################################
# Window Rules
###############################################################################
for_window [floating] move position center
for_window [instance="Godot_Engine"] floating enable, move position center
for_window [class="discord"] move to workspace $ws4
for_window [class="vesktop"] move to workspace $ws4
for_window [class="zen"] move to workspace $ws2
for_window [class="obsidian"] move to workspace $ws6

# Window Border Colors
###############################################################################
# class                 border  bckgrd  text    indicator child_border
client.focused          #9c7899 #985577 #ffffff #2e9ef4   #9c7899
client.focused_inactive #333333 #5f676a #ffffff #484e50   #5f676a
client.unfocused        #333333 #222222 #888888 #292d2e   #222222
client.urgent           #2f343a #900000 #ffffff #900000   #900000
client.placeholder      #000000 #0c0c0c #ffffff #000000   #0c0c0c
client.background       #ffffff

###############################################################################
# Bar Configuration
###############################################################################
bar {
    status_command SCRIPT_DIR=~/.config/i3blocks/scripts i3blocks
    tray_output primary
    colors {
        background #222222
        statusline #bbbbbb
        separator  #666666
        focused_workspace  #9c7899 #985577 #ffffff
        active_workspace   #333333 #5f676a #ffffff
        inactive_workspace #222222 #222222 #888888
        urgent_workspace   #2f343a #900000 #ffffff
    }
}
